services:
  python:
    build:
      context: .
    container_name: python
    ports:
      - "${PYTHON_PORT}:5000"
    volumes:
      - .:/app
      - ../../logs:/usr/src/app/logs
    networks:
      - app-network

networks:
  app-network:
    external: true
